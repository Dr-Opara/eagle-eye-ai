'use client';
import Link from 'next/link';import {usePathname} from 'next/navigation';import {nav} from '@/lib/data';import {useEffect,useState} from 'react';
export function Shell({children}:{children:React.ReactNode}){const pathname=usePathname();const[clock,setClock]=useState('');useEffect(()=>{const tick=()=>setClock(new Date().toISOString().slice(11,19)+' UTC');tick();const i=setInterval(tick,1000);return()=>clearInterval(i)},[]);return <main className="app-shell"><header className="topbar"><Link href="/" className="brand"><span className="eagle">◢</span><span><b>EagleEye</b><small>AI GLOBAL CYBER INTELLIGENCE</small></span></Link><nav className="nav">{nav.map(([n,h])=><Link key={h} href={h} className={pathname===h?'active':''}>{n}</Link>)}</nav><div className="top-actions"><Link className="support-link" href="/support">Support</Link><Link className="demo-link" href="/demo">Request Demo</Link><div className="status"><span className="pulse green"/>LIVE <b>{clock}</b></div></div></header><div className="integrity-ribbon"><b>INTELLIGENCE INTEGRITY</b><span>Live public feeds, OSINT claims, and simulated product telemetry are labeled separately. EagleEye never treats a map animation as proof of a live attack.</span></div>{children}</main>}
